import { defineConfig } from "vite";

// IMPORTANT:
// - If this repo is served from https://TetSigma.github.io/physics-lorentz,
//   keep base as "/physics-lorentz/" (the repo name).
// - If you rename the repo, update base accordingly.

export default defineConfig({
  base: "/physics-lorentz/",
});

